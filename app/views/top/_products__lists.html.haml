.products__lists
  - @items.each do |item|
    = link_to item_path(item.id), class: 'productGroup' do
      .product
        .product__image
          = image_tag item.images.last.image.url, :size =>'220x150', class:'product__image--pict'
        .product__info
          .product__info--name
            = item.name
          .product__info--details
            %ul.detailList
              %li.detailList__price
                = item.price
                円
              %ul.detailList__box
                =icon('fas', 'star',class: 'star-icon')
                %li.count
                  0
            .tax
              (税込)